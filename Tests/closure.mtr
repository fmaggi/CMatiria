fn main()
{
    (Float) -> Int x := help();
    print(x(45.0));

    fn c(Int y) -> Int
    {
        return y;
    }

    test(hello);
    test(c);
}

fn help() -> (Float) -> Int
{
    x := 3;
    fn c(Float y) -> Int
    {
        return x + 3;
    }
    x := 45;
    return c;
}

fn test((Int) -> Int x) {
    print(x(3));
}

fn print(Any x) ...


fn hello(Int x) -> Int {
    return x + 2;
}
